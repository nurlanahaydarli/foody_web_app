import { createNamespace, isStringElement, isArrayElement, isObjectElement, includesClasses } from '@swagger-api/apidom-core';
import * as openApi3_1Predicates from "../predicates.mjs";
import openApi3_1Namespace from "../namespace.mjs";
const createToolbox = () => {
  const namespace = createNamespace(openApi3_1Namespace);
  const predicates = {
    ...openApi3_1Predicates,
    isStringElement,
    isArrayElement,
    isObjectElement,
    includesClasses
  };
  return {
    predicates,
    namespace
  };
};
export default createToolbox;